apiVersion: v2
name: kube-prometheus-stack
description: Wrapper chart to install kube-prometheus-stack via Argo CD
type: application
version: 0.1.0

dependencies:
  - name: kube-prometheus-stack
    version: 77.5.0      # <- bump to an available version
    repository: https://prometheus-community.github.io/helm-charts
    alias: kps